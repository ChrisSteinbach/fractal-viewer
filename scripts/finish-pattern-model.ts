/**
 * Compatibility entry point for the pre-renderer evidence harness. The
 * accepted V3 arithmetic now lives with the document domain so the prototype
 * and downstream shader ports cannot acquire separate definitions.
 */
export * from "../src/fractal/surface-pattern";
